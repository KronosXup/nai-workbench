# Copy to .env, then set the Gate URL and its existing Docker network.
NODE_IMAGE=node:22-alpine
PYTHON_IMAGE=python:3.12-slim
WORKBENCH_GATE_URL=http://gate:8000
GATE_DOCKER_NETWORK=gate_default
WORKBENCH_BIND=127.0.0.1
WORKBENCH_PORT=8787
WORKBENCH_IMAGE_TAG=gate
